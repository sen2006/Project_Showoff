using UnityEngine;

public class LoadImages : MonoBehaviour
{
    void Start()
    {
        GalleryLoader.Load();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
